Read harness/worker_contract.md first and obey it strictly.

# Task M5-glob-12: Koszul naturality — the transition inputs, signs pinned

Context: `harness/reports/M5-glob-11_blocked.md` (READ FIRST — the VERBATIM chain-rule + transition inputs AND the sign issue under this repo's `γ'' = −Γ(γ)(γ',γ')` convention). PROVEN: the bridge (transition + chain rule ⟹ reanchor, `ChristoffelTransition.lean`). THE PRODUCER: (1) THE METRIC TRANSITION: the blended chart metrics at two anchors are pullbacks of the SAME `g` — their transition under the chart change `σ` is the pullback law (`CovariantDerivative.chartMetric`'s definition — the transition is definitional-modulo chart-composition algebra; `ChartIdentification/ChartTransport.lean` may have it); (2) THE CHRISTOFFEL TRANSITION via LEVI-CIVITA UNIQUENESS: `LeviCivitaUniqueness.lean` (READ its exact statement — uniqueness of metric-compatible torsion-free connections in a chart): the σ-transported source Christoffel field is metric-compatible + torsion-free for the TARGET chart metric (compatibility transports through the pullback law; torsion-freeness through `D²σ` symmetry — `ContDiff.isSymmSndFDerivAt`) ⟹ equals the target Christoffel field — WITH THE REPO'S SIGNS: ⚠️ PIN the final transition identity numerically (Euclidean chart pair or the sphere's two stereographic charts — `RoundSphereChart.lean` has explicit data) BEFORE the abstract proof; (3) the chain-rule input (`D(σ∘γ)'' = Dσ γ'' + D²σ(γ',γ')` — `HasFDerivAt.comp` second-order — the two-variable machinery). FEED the bridge → the reanchor law → the naturality chain → `RigidStepCompatibleWith` → THE CHAIN FIRES. Strict-partial; ONE isolated statement max. Report `harness/reports/M5-glob-12_{done|blocked}.md` with the pin outcome.

Deliverables in a NEW file `Poincare/Global/KoszulNaturality.lean` (do NOT edit existing files, incl. `Poincare.lean`).

No vacuous wrappers. Verify: `lake build Poincare.Global.KoszulNaturality` and report the actual result. Commit your work.
