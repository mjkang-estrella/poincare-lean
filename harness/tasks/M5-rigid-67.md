Read harness/worker_contract.md first and obey it strictly.

# Task M5-rigid-67: the speed-s package — unit speed was never available

Context: `harness/reports/M5-rigid-66_blocked.md` (READ FIRST): unit-G velocities cannot fit the PL ball (`‖u‖ < δ`) — normalizing to speed 1 and staying in the small-data regime are INCOMPATIBLE; route (a) is dead. ROUTE (b): generalize the unit-speed-demanding package layer to SPEED-s. FIND the exact `hunit` consumer (per rigid-64/65's reports — READ them; which lemma in `TargetPackage/SourcePackage/CartanIsometryTheorem.lean` demands speed 1): the demand comes from the oscillator frequency — at constant speed `s`, the covariant Jacobi equation is `J'' = −s²·J` with solutions `sin(s·t)/s`-shaped, and the norm system's pinned solution becomes `(sin²(st), s·sin(st)cos(st), s²cos²(st))`-shaped with initial `(0, 0, s²|w|²)`... derive carefully. THE MACHINERY EXISTS: the scalar ODE uniqueness is frequency-generic (`CoefficientEvolution/JacobiIntegrated.lean` — check whether `closed_norm_system_eq_pinned_on_Icc` hardcoded frequency 1; if so, ADD the speed-s variant by the same proof with the rescaled pinned functions); the angle machinery (`normalizedRescaledAngle`, `SourcePackage.lean`) absorbs the `s` into the final sin² factors (the feeds' shape `sin²(sT)·⟨T⁻¹a, T⁻¹a'⟩` is EXACTLY what the chain consumes — rigid-61's target package already produced this shape at the hosted data! CHECK: maybe only the SOURCE mirror hardcoded hunit — then only it needs the speed-s replay). Deliver the speed-s package layer (additive edits to `TargetPackage/SourcePackage.lean` allowed — new variants only) → discharge at the hosted (small-velocity) data via `SpeedPackage.lean`'s speed values → the feeds → 🎯 `cartanMap_isLocalIsometry`. If ONE resists, isolate verbatim.

Deliverables in a NEW file `Poincare/Global/SpeedGeneric.lean` (+ sanctioned additive edits). Report `harness/reports/M5-rigid-67_{done|blocked}.md`.

No vacuous wrappers. Verify: `lake build Poincare.Global.SpeedGeneric Poincare.Global.SourcePackage Poincare.Global.TargetPackage` (ALL must pass) and report the actual result. Commit your work.
