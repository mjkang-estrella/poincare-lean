Read harness/worker_contract.md first and obey it strictly.

# Task M5-glob-26: LC-naturality under the Cartan map — geodesics to geodesics

Context: `harness/reports/M5-glob-25_blocked.md` (READ FIRST). ORCHESTRATOR ANALYSIS: 4 tasks orbited "the old map at the new anchor" — the TRUE core is GEODESIC PRESERVATION for general (transverse) directions, and the proof is the SAME LC-uniqueness argument as the transition law (glob-13..18), with the chart transition σ REPLACED by the Cartan map's chart representation `F := cartanChartMap` and the metric-pullback germ REPLACED by THE PROVEN PULLBACK IDENTITY (`RigidityComplete.cartanMap_isLocalIsometry`'s conclusion / `IsometryConsumers.lean`'s carried pullback): (1) differentiate the pullback identity (the hdiff pattern — `PullbackDifferentiate.lean`'s abstract lemma applies with `sigma := F`, `D := the differential` — the smoothness of F from the strict derivatives); (2) the algebraic step (`DifferentiatedCompat.lean`'s theorem is METRIC-GENERIC — check its quantification; apply with the F-data); (3) torsion-freeness of the F-transported field (`TransitionLaw.lean`'s symmetry lemma — generic); (4) LC uniqueness (`TransitionLawFires.lean`'s pattern) ⟹ THE F-TRANSITION LAW: the target Christoffels pull back to the source ones under F (signed); (5) ⟹ F maps source geodesics to target geodesics (the geodesic ODE transports — the `SideConditions/ChainRuleInput.lean` pattern with F); (6) ⟹ the old map's exp-naturality AT ANY point of its ball (both sides are target geodesics with equal initial data — PL uniqueness) → feed `TargetRayIdentity/TwoBridges` → 🎯 `RigidStepCompatibleWith`. Strict-partial per stage; ONE isolated statement max. Report `harness/reports/M5-glob-26_{done|blocked}.md`.

Deliverables in a NEW file `Poincare/Global/LCNaturality.lean` (do NOT edit existing files, incl. `Poincare.lean`).

No vacuous wrappers. Verify: `lake build Poincare.Global.LCNaturality` and report the actual result. Commit your work.
