Read harness/worker_contract.md first and obey it strictly.

# Task M5-rigid-53: haccCollapse — the Christoffel corrections at the hosted state

Context: `harness/reports/M5-rigid-52_blocked.md` (READ FIRST — the VERBATIM `haccCollapse` statement). PROVEN LAST ROUND: the direct-from-identity action equation (`linearizedEndpointCLM_apply_sourceScaledNormalVector_of_acceleration_identity`, `CartanTheIsometry.lean`) modulo `haccCollapse` — the explicit Christoffel correction-collapse at the hosted state. ANALYZE THE COLLAPSE: the corrections are `Γ`-pairings of the geodesic velocity with the Jacobi/harmonic state (the `K + Γ(z)(V,J)` terms from the state bridge + whatever the linearized RHS reorganizes). WHERE THE COLLAPSE LIVES: (a) the linearized RHS DEFINITION (`GeodesicLinearized.lean`) — its Γ-structure may literally BE the correction terms (term-match: the collapse is definitional unfolding + `simp`/`ring` reorganization); (b) the state-bridge bookkeeping (`JacobiNormClose.lean`'s proof did this rearrangement pointwise — extract the rearrangement steps); (c) if a genuine cancellation is needed (symmetric Γ pairings), the symmetry lemmas (`chartChristoffelField` symmetry, `GeodesicChart/LeviCivita*.lean`). ⚠️ If the collapse demands an identity that is NOT a rearrangement (e.g. a curvature-term vanishing), PIN it against the round sphere's explicit Christoffels (`RoundSphereChartMetric.lean`) BEFORE proving — the refutation protocol. PROVE `haccCollapse`, feed rigid-52's theorems → the action equations UNCONDITIONAL → continue the composition as far as it closes (equivalence upgrade → blocks → the bridge → 🎯 `cartanMap_isLocalIsometry`).

Deliverables in a NEW file `Poincare/Global/ChristoffelCollapse.lean` (do NOT edit existing files, incl. `Poincare.lean`). Strict-partial: the collapse alone is the prize; ONE isolated statement max. Report `harness/reports/M5-rigid-53_{done|blocked}.md`.

No vacuous wrappers. Verify: `lake build Poincare.Global.ChristoffelCollapse` and report the actual result. Commit your work.
