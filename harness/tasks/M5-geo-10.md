Read harness/worker_contract.md first and obey it strictly.

# Task M5-geo-10: GOAL 9 — germ re-anchoring (the extension step), not general-overlap naturality

Context: `Poincare/Global/GeodesicOverlap.lean` (report `harness/reports/M5-geo-8_blocked.md`) scaffolded transition transport and isolated a "chart-overlap Christoffel naturality" blocker. ORCHESTRATOR RE-SCOPE: general-overlap naturality is likely FALSE as stated for the blended fields — `chartChristoffelField g y₀` represents `g.leviCivita` only on `y₀`'s cutoff-1 zone (the blended metric is a genuine mix away from the anchor). What geodesic EXTENSION actually needs is the RE-ANCHORING law: a germ, at a small positive time, continues as the germ through its own current point. Both anchors are then automatically in each other's good zones for small `t₀`. Read the geo-8 report and reuse its scaffold (`chartTransition*`, `chartTransitionState_eventually_solves_of_components`, germ component lemmas).

Deliverables, in a NEW file `Poincare/Global/GeodesicReanchor.lean` (do NOT edit any existing file, incl. `Poincare.lean`):

1. NEAR-ANCHOR COEFFICIENT REPRESENTATION: make explicit (as lemmas) that on the anchor's cutoff-1 zone the blended chart metric equals the transported metric of `g` (from the cutoff spec `cutoff_eventuallyEq_one` + `blendedChartMetric` definition — mine `Poincare/Global/GeodesicTransport.lean` and the underlying `CovariantDerivative` chart machinery), hence `chartChristoffelField g x₀` is, near `extChartAt I x₀ x₀`, the genuine transported Christoffel of `g` in `x₀`'s chart.
2. TRANSITION LAW IN THE DOUBLE-GOOD ZONE: for `y₀` close to `x₀` (both within each other's cutoff-1 zones — quantify via an honest neighborhood hypothesis), prove the transported-solution law: the `chartTransitionState` of an `x₀`-chart geodesic solution eventually solves `y₀`'s system (discharging the component hypotheses of `chartTransitionState_eventually_solves_of_components` in this zone). Route: both chart Christoffels are transported Christoffels of the SAME `g` (deliverable 1) near the relevant points; derive the coefficient transformation from the two `chartLeviCivita_eventuallyEq_closed` bridges + `levi_civita_unique`-style identification on common test sections, or by the direct chain-rule computation on the transition — pick the route that closes; document.
3. RE-ANCHORING COROLLARY: for the germ `c := geodesicGermAt g x₀ v₀` and small `t₀ > 0` (with `y₀ := c t₀` in the double-good zone), the shifted curve `s ↦ c (t₀ + s)` agrees near `s = 0` with `geodesicGermAt g y₀ v₁` for the transported velocity `v₁` (uniqueness at anchor `y₀` after step 2). This is the geodesic-extension step.
4. Report `harness/reports/M5-geo-10_{done|blocked}.md`: signatures + next decomposition (uniform extension via compactness → geodesic completeness). Strict-partial with a sharper isolated blocker is valid.

No vacuous wrappers; hypotheses used or removed. Verify: `lake build Poincare.Global.GeodesicReanchor` and report the actual result. Commit your work.
