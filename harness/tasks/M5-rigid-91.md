Read harness/worker_contract.md first and obey it strictly.

# Task M5-rigid-91: feed the membership — the assembly completes

Context: `harness/reports/M5-rigid-90_done.md` + `M5-rigid-88_blocked.md` (READ BOTH). THE MEMBERSHIP EXISTS: `GronwallMembership.lean` — non-circular Grönwall closed-ball membership, `q`-centered and fixed-radius FEED-SHAPED lemmas, the bounded-`q` radius handoff. THE ASSEMBLY: at the hosted datum, feed the membership into `SolutionsFeed`'s hypotheses (the exact `hmem`-shaped fields from rigid-88's report), completing the transverse blocks; the rest of the inventory stands ready (`IsometryComplete.lean`'s proven feeds: enriched packages → orthogonality + source block; radial `RayIdentification/SpeedReconcile`; speeds `SpeedPackage`; the consumer chain `BundleDischarge → CombinedFeed → CorrectedRadial T²`). 🎯 `cartanMap_isLocalIsometry` — curvature-only. If ONE hypothesis genuinely cannot be fed, isolate verbatim.

Deliverables in a NEW file `Poincare/Global/AssemblyDone.lean` (do NOT edit existing files, incl. `Poincare.lean`). Report `harness/reports/M5-rigid-91_{done|blocked}.md`.

No vacuous wrappers. Verify: `lake build Poincare.Global.AssemblyDone` and report the actual result. Commit your work.
