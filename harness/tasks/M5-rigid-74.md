Read harness/worker_contract.md first and obey it strictly.

# Task M5-rigid-74: the ray identification — radial Ψ IS the ray derivative

Context: `harness/reports/M5-rigid-73_blocked.md` (READ FIRST — the verbatim radial hypotheses). PROVEN: the corrected-radial consumer chain (`CorrectedRadial.lean`, 617 lines — plain `T²·speed²` radial scale, split assembly, feeds, final consumer). THE ONE GEOMETRIC THEOREM: the radial linearized endpoint `Ψ_v(T).1` = the ray-derivative value (`T·(chart velocity at the endpoint)`-shaped — read the exact demanded form). THE PROOF ROUTE: the ray/scaling law `germ(s·v, t) = germ(v, s·t)` (`ExponentialRayLaw/ExponentialRayLawFull.lean`, `ExponentialGerm.lean` — the proven homogeneity) — DIFFERENTIATE in `s` at `s = 1`: the left side's s-derivative is the radial linearized solution (the flow derivative in the initial-velocity direction `v` — `GeodesicFlowDerivative.lean`'s derivative IS this; check the direction-v case), the right side's is `t·(time derivative) = t·γ'(t)` (the geodesic velocity — `geodesicGermAt_chart_hasDerivAt`). Both sides solve the same linearized ODE with initial data `(0, v)` — LINEARIZED UNIQUENESS (`linearODE_solution_uniqueOn_Icc`) identifies them WITHOUT differentiating the law directly if simpler: verify the ray-derivative curve `t ↦ (t·γ'(t), γ'(t) + t·γ''(t))`-shaped solves the linearized system (γ'' from the geodesic equation — the Γ-terms match the linearized RHS at the radial state — CHECK by substitution) with initial `(0, v)`. Then the endpoint pairing = `T²·speed²` (constant speed) → the radial blocks (source + aligned target) → feed `CorrectedRadial.lean`'s consumer with ALL blocks → 🎯 `cartanMap_isLocalIsometry`. If ONE resists, isolate verbatim.

Deliverables in a NEW file `Poincare/Global/RayIdentification.lean` (do NOT edit existing files, incl. `Poincare.lean`). Report `harness/reports/M5-rigid-74_{done|blocked}.md`.

No vacuous wrappers. Verify: `lake build Poincare.Global.RayIdentification` and report the actual result. Commit your work.
