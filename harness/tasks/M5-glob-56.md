Read harness/worker_contract.md first and obey it strictly.

# Task M5-glob-56: the hosted third-variation family — export and continuity

Context: `harness/reports/M5-glob-55_blocked.md` (READ FIRST — the VERBATIM hosted family/continuity demands). PROVEN: the endpoint projection (`TowerClosed.lean`); the residual theorem (`DoublyResidual.lean`); the PL package (`ThirdVariation.lean`). THE EXPORT (the `LinearizedFamilyExport.lean`/glob-42 pattern at level three): (1) the hosted third-variation solution family on the PL closed ball (the PL fixed-point construction — the same replay); (2) the family's ODE/initial facts feeding `DoublyResidual`'s `thirdVariation_data` at the hosted datum; (3) CONTINUITY of the third-variation endpoints in the base (the Grönwall dependence — the `AugmentedDependence` pattern at the third-variation system, or the endpoint difference bound from the residual layer). ASSEMBLE → `ContDiffAt ℝ 1 (fderiv e field)` → `CanonicalC1/LevelThreeFeed/TowerCloses` fire → 🎯 THE F-TRANSITION LAW UNCONDITIONAL. Strict-partial; ONE isolated statement max. Report `harness/reports/M5-glob-56_{done|blocked}.md`.

Deliverables in a NEW file `Poincare/Global/ThirdFamily.lean` (do NOT edit existing files, incl. `Poincare.lean`).

No vacuous wrappers. Verify: `lake build Poincare.Global.ThirdFamily` and report the actual result. Commit your work.
