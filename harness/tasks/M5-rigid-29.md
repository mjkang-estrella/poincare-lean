Read harness/worker_contract.md first and obey it strictly.

# Task M5-rigid-29: the coefficient bridge — the pairing becomes the expansion

Context: `harness/reports/M5-rigid-28_blocked.md` (READ FIRST). PROVEN: the bilinear endpoint pairing `actual_jacobi_pairing_eq_pinned_of_quadratic_and_linearized_unique` (`CartanIsometryPackage.lean`) + the actual quadratic identities (`CartanIsometryTheorem.lean`) + radial/cross facts (constant speed `GeodesicSpeed/GeodesicLengthFinal.lean`; integrated Gauss `GaussLemmaIntegrated.lean`) + the conjugation (`CartanNormalCoords.lean`). REMAINING: the "source endpoint expansion bridge" — package the three pairing facts (radial-radial, radial-transverse, transverse-transverse) into the coefficient statement the pullback consumes. TWO OPTIONS (pick what closes; document): (a) target the EXISTING punctured/source-owned bundle shapes (`CartanLocalIsometry/CartanWeightInvariant.lean` — read which bundle the weight-canceling consumer needs and construct it from the pairing facts; the source-owned weight in exp charts is `sin²t/t²`-shaped — CHECK against the bundle's normalization); (b) BYPASS the old bundles: state the exp-chart pullback DIRECTLY — for `u, u'` decomposed at `v ≠ 0` (`CartanPullback.lean`'s Gram algebra), `g`-pairing of `D(expAt)` images = the three-term formula — on BOTH sides, then the conjugation gives `cartanMap`'s pullback equality outright; the old consumers become unnecessary. Option (b) is likely shorter — the conjugation makes the map linear in exp charts, so the pullback IS the statement "both exp-chart coefficient matrices agree through L", which the pairing facts + alignment intertwining state directly.

Deliverables in a NEW file `Poincare/Global/CartanCoefficientBridge.lean` (do NOT edit existing files, incl. `Poincare.lean`): the bridge/package per the chosen option + 🎯 THE LOCAL ISOMETRY (exp-chart pullback equality for `cartanMap`, clean statement). Strict-partial; ONE isolated statement max. Report `harness/reports/M5-rigid-29_{done|blocked}.md`.

No vacuous wrappers. Verify: `lake build Poincare.Global.CartanCoefficientBridge` and report the actual result. Commit your work.
