Read harness/worker_contract.md first and obey it strictly.

# Task M2-deturck-1: GOAL 9 — honest DeTurck vocabulary at the Global layer

Context: `Poincare/Global/ShortTimeInterface.lean` (task M2-scope-1, report `harness/reports/M2-scope-1_assets.md`) landed `RicciFlowShortTimeExistence3`. Next per its roadmap: the DeTurck reduction statement layer. CRITICAL CONSTRAINT: the legacy interface layers (`Poincare/RicciFlowInterface.lean`, `Poincare/AnalyticFoundation.lean`, `Poincare/Dependencies*.lean`, `Poincare/Assembly.lean`, `Poincare/ProofProgress/*`, and their `...Data`/`Has...` vocabulary) are QUARANTINED as vacuous legacy scaffold per `INTEGRITY_ASSESSMENT.md` — do NOT import, reference, or extend them. Build exclusively on the genuine `Global/` vocabulary: `IsClosedRicciFlowSolutionAt` (`Global/RicciFlow.lean`), `CovariantDerivative.ricciTraceAt`, `g.leviCivita` (`Global/Curvature.lean`), the M3 variation vocabulary (`Global/MetricVariation.lean`, `Global/ScalarVariation.lean`).

Deliverables, in a NEW file `Poincare/Global/DeTurck.lean` (do NOT edit existing files, incl. `Poincare.lean`):

1. LIE DERIVATIVE OF THE METRIC along a vector field, at the pointwise regularity style of the repo: for `g : ClosedSmoothRiemannianMetric n M`, a tangent field `W`, and a point `x` with a suitable regularity hypothesis on `W` at `x` (mirror the repo's `DerivRegularAt`/`MDiffAtTangentField` plumbing — reuse, don't reinvent), define `lieDerivMetricAt g W x u w : ℝ` as the torsion-free LC form `g.inner x ((g.leviCivita W x) u) w + g.inner x u ((g.leviCivita W x) w)` (adapt to the actual `CovariantDerivative` application API; document the final spelling). Prove: symmetry in `u w`, and vanishing for the zero field (use the connection's linearity in the section — locate the zero/linearity lemma in the `CovariantDerivative` API and cite it; if absent, prove it).
2. RICCI–DETURCK EQUATION, pointwise: `IsClosedRicciDeTurckSolutionAt (gt : ℝ → ClosedSmoothRiemannianMetric n M) (Wt : ℝ → ∀ y, TM y) (t₀ : ℝ) (x : M) : Prop` in the same `deriv (fun t ↦ (gt t).inner x (Z x) w)`-against-test-fields style as `IsClosedRicciFlowSolutionAt`/`isClosedRicciFlowSolutionAt_of_metric` (`Global/RicciFlow.lean:40,72`): RHS `= -2 * ricciTraceAt … + lieDerivMetricAt (gt t₀) (Wt t₀) x …`. Keep hypotheses-shape parallel to the existing flow predicate so reductions are stateable.
3. REDUCTION: if `Wt t₀` is the zero field (or its covariant derivative vanishes at `x` — choose the honest minimal hypothesis), then `IsClosedRicciDeTurckSolutionAt gt Wt t₀ x ↔ IsClosedRicciFlowSolutionAt gt t₀ x`. This is the anchor theorem — the DeTurck equation genuinely extends the flow equation.
4. INTERFACES + COMPOSITION: `RicciDeTurckShortTimeExistence3 (M)` (∀ `g₀` ∃ `T > 0`, ∃ `(gt, Wt)`, `gt 0 = g₀` ∧ DeTurck clause on `Ico 0 T`) and `DeTurckPullbackToRicciFlow3 (M)` (: every short-time DeTurck family admits a Ricci-flow family with the same initial metric — the honest gauge-pullback wall, named). Prove `ricciFlowShortTimeExistence3_of_deTurck_of_pullback : RicciDeTurckShortTimeExistence3 M → DeTurckPullbackToRicciFlow3 M → RicciFlowShortTimeExistence3 M`.
5. STATIC SANITY: via the reduction theorem and the existing static Ricci-flat witness (`isClosedRicciFlowSolutionAt_const_of_ricciFlat` / `static_ricciFlat_flowClause`), show the DeTurck clause is satisfiable in the static Ricci-flat, zero-field case.
6. Report `harness/reports/M2-deturck-1_{done|blocked}.md`: final signatures, the DeTurck-vector-field construction (trace of the connection difference against a background metric) as the isolated NEXT task with a precise statement sketch.

No vacuous wrappers; hypotheses used or removed. Verify: `lake build Poincare.Global.DeTurck` and report the actual result. Commit your work.
