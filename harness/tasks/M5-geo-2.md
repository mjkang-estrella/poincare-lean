Read harness/worker_contract.md first and obey it strictly.

# Task M5-geo-2: GOAL 9 — chart Christoffel fields and local geodesics for `g.leviCivita`

Context: M5-geo-1 landed `Poincare/Global/GeodesicChart.lean` (chart-level geodesic ODE: `geodesicFlowField`, Picard–Lindelöf existence `exists_geodesicFlowField_solution`, Grönwall uniqueness `geodesicFlowField_eventuallyEq_of_contDiffAt`, component readings) and the roadmap `harness/reports/M5-geo-1_assets.md`. This task is roadmap steps 1–2 (+3 if within reach): connect the abstract ODE layer to the actual Levi-Civita connection of a closed metric.

Deliverables, in a NEW file `Poincare/Global/GeodesicTransport.lean` (do NOT edit any existing file, incl. `Poincare.lean`):

1. CHART CHRISTOFFEL FIELD: for `g : ClosedSmoothRiemannianMetric n M` and an anchor `x₀ : M`, define the chart-side Christoffel field of type `E → E →L[ℝ] E →L[ℝ] E` with `E := ClosedSmoothModel n`, representing `g.leviCivita` transported through `extChartAt (closedSmoothModelWithCorners n) x₀`. REUSE the repo's existing transported-connection machinery instead of re-inventing: survey `Poincare/Global/LeviCivitaTransport.lean` (chart-transport API + uniqueness bridges), `Poincare/Global/LeviCivitaRegularity.lean` (chart-side hom smoothness, `inCoordinates` gluing), `Poincare/ChartTransport.lean`, `Poincare/ModelChristoffel.lean` (model-space Christoffel and its regularity at any order). If a transported Christoffel/connection-coefficient object already exists, build on it and say so in the report.
2. REGULARITY: prove the field from (1) is `ContDiffAt ℝ 1` at (a neighborhood of) the anchor's chart image — the repo's `leviCivita_contMDiff₂` (`Poincare/Global/Curvature.lean:50`) and the chart-side smoothness lemmas of the regularity chain are the intended sources.
3. LOCAL GEODESIC EXISTENCE ON `M`: instantiate `exists_geodesicFlowField_solution` to conclude: for every `x₀ : M` and initial velocity `v₀` (chart representation is acceptable — state honestly what object carries the velocity), there exist `ε > 0` and a curve `γ : ℝ → ClosedSmoothModel n` solving the chart geodesic system with `γ 0 = extChartAt … x₀ x₀`-anchored initial data, together with the pulled-back curve `c : ℝ → M`, `c t = (extChartAt … x₀).symm (γ t)`, satisfying `c 0 = x₀` and (for small `t`) `c t ∈ (extChartAt … x₀).target`-preimage — i.e., an honest "local geodesic through every point in every direction" statement at the chart level. Packaging freedom sanctioned; the mathematical content (existence through every point/direction, driven by `g.leviCivita`'s coefficients, not an arbitrary field) is frozen.
4. If step 3's full packaging blocks, steps 1–2 committed + a precise blocked report is a valid outcome. Report: `harness/reports/M5-geo-2_{done|blocked}.md` with final signatures + next decomposition (chart-overlap uniqueness, exponential germ).

No vacuous wrappers; every hypothesis used or removed. Verify: `lake build Poincare.Global.GeodesicTransport` and report the actual result. Commit your work.
