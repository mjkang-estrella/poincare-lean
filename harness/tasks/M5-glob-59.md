Read harness/worker_contract.md first and obey it strictly.

# Task M5-glob-59: THE ASSEMBLY — the F-transition law lands

Context: `harness/reports/M5-glob-58_done.md` (READ FIRST). BOTH LASER FACTS PROVEN: `exists_hosted_thirdVariation_solution_family_on_paired_base` + `chartChristoffel_thirdVariation_endpoint_gronwall_bound` (`OmegaGronwall.lean`). THE COMPLETE INVENTORY: the residual (`DoublyResidual.lean`), the projection (`TowerClosed.lean`), the continuity payload (Grönwall ⟹ Lipschitz ⟹ continuous), the bridges (`EndpointContinuity → CanonicalC1 → LevelThreeFeed → TowerCloses`), the consumers (`PackageLands/ExpChartC2/ContDiffTwo/EndpointBridge/FTransitionDone`). ASSEMBLE at the hosted datum: Ω's data → the residual → the endpoint derivative; the Grönwall bound → continuity; → `ContDiffAt 1` canonical field → the bridge chain end to end → 🎯 THE F-TRANSITION LAW UNCONDITIONAL (source; the SPHERE side by the same generic route — instantiate at roundSphereMetric3). If ONE shape resists, isolate verbatim. Report `harness/reports/M5-glob-59_{done|blocked}.md`.

Deliverables in a NEW file `Poincare/Global/TransitionLands.lean` (do NOT edit existing files, incl. `Poincare.lean`).

No vacuous wrappers. Verify: `lake build Poincare.Global.TransitionLands` and report the actual result. Commit your work.
