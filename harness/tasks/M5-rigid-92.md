Read harness/worker_contract.md first and obey it strictly.

# Task M5-rigid-92: the common time — one T on both sides

Context: `harness/reports/M5-rigid-91_blocked.md` (READ FIRST — the verbatim mismatch: `BaseCurvePackage … Ts …` vs `… Tt …` with no `Ts = Tt`). PROVEN: both enriched transverse blocks with membership (`AssemblyDone.lean`, 746 lines). THE FIX: the hosted times are DEFINED from the working data — source `Ts` from `(δs, v)`, target `Tt` from `(δt, L v)` (`CartanHomogeneity.lean` construction: `T = ‖v‖/(δ/2)`-shaped). ROUTE (a) COMMON-TIME RE-EXPORT: replay `EnrichedCascade.lean`'s common theorem CHOOSING the same working scale on both sides — take `δ := min δs δt` (both sides' hypotheses hold at a smaller δ — the balls nest) and the SAME Euclidean norm datum: `‖L v‖ = ‖v‖`? — CAREFUL: `L` preserves the ANCHOR METRIC norms, not necessarily the raw Euclidean norm — CHECK `TangentAlignment`'s fields (`CartanMap.lean`); if only metric norms are preserved, choose the working times from the METRIC norm (T := metric-norm-based — the hosted machinery may need the metric-norm variant; OR route (b) RETIMING: the pairing formulas rescale between `Ts` and `Tt` by the homogeneity lemmas (`Ψ` smul in time via the germ scaling `ExponentialRayLawFull.lean`) — convert the target fields to `Ts` (the sin factors retime as `sin(s·T)` with the product `s·T` INVARIANT under the rescaling — check: speed·time = metric-norm of v, alignment-invariant! So the sin arguments MATCH even if T's differ — the retiming may be trivial at the formula level). Pick what closes; document. Then the common-time consumer fires → 🎯 `cartanMap_isLocalIsometry` (curvature-only). If ONE resists, isolate verbatim.

Deliverables in a NEW file `Poincare/Global/CommonTime.lean` (do NOT edit existing files, incl. `Poincare.lean`). Report `harness/reports/M5-rigid-92_{done|blocked}.md`.

No vacuous wrappers. Verify: `lake build Poincare.Global.CommonTime` and report the actual result. Commit your work.
