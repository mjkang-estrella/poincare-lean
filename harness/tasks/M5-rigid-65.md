Read harness/worker_contract.md first and obey it strictly.

# Task M5-rigid-65: hunit — the speed package for the actual hosted curves

Context: `harness/reports/M5-rigid-64_blocked.md` (READ FIRST — the VERBATIM `hunit` field). PROVEN: the instantiation helpers (`IsometryInstantiate.lean`: germ packaging, derivative upgrades). THE ONE FIELD: the unit-speed (or speed-value) package hypothesis for the actual hosted base curves. THE INGREDIENTS: CONSTANT SPEED is proven (`chart_geodesic_speed_constantOn`, `GeodesicSpeed.lean`; hosted forms in `GeodesicLengthFinal.lean`) — the hosted geodesic through `(u,T)` has constant `g`-speed = the anchor value `G(x₀-anchor)(u,u)`; if `hunit` demands speed EXACTLY 1, check WHERE the package uses it — likely the oscillator frequency normalization: the covariant Jacobi equation frequency is speed² (constant curvature 1 ⟹ `J'' = −speed²·J` in the affine parameter) — the packages' `sin` pinning at frequency 1 needs unit speed OR the `normalizedRescaledAngle` machinery (rigid-63, `SourcePackage.lean`) absorbs general speed — READ how the package consumed `hunit` and either: (a) discharge with the constant-speed value + a normalization of `u` (choose the hosted working velocity with `G`-norm 1 instead of Euclidean δ/2 — an alternative hosting via the anchor-metric normalization; the alignment preserves it on the target side by `TangentAlignment`'s intertwining), or (b) generalize the package additively to speed-s with the angle machinery. Then the feeds instantiate → 🎯 `cartanMap_isLocalIsometry` (hypotheses: curvature + anchors + alignment ONLY). If ONE sub-fact resists, isolate verbatim.

Deliverables in a NEW file `Poincare/Global/SpeedPackage.lean` (+ sanctioned ADDITIVE edits to `SourcePackage/TargetPackage.lean` ONLY if route (b) — new variants, no changes to existing statements). Report `harness/reports/M5-rigid-65_{done|blocked}.md`.

No vacuous wrappers. Verify: `lake build Poincare.Global.SpeedPackage` (+ the touched modules) and report the actual result. Commit your work.
