Read harness/worker_contract.md first and obey it strictly.

# Task M5-rigid-13: the mediating constructor — expansion hypotheses from interval facts

Context: `harness/reports/M5-rigid-12_blocked.md`: rigid-11's `cartanMap_isLocalIsometry_on_normalBall` (`Poincare/Global/CartanLocalIsometry.lean`) requires endpoint metric-expansion hypotheses in a packaged shape, while the proven metric-generic facts (`CartanDifferential.lean` differential surface, `JacobiOscillator.lean` sin formula, `GaussLemmaIntegrated.lean`, `GeodesicSpeed.lean`) live at PL/interval/cutoff-scoped level. THE FIX (geo-13 precedent): SANCTIONED EXCEPTION — you MAY edit `Poincare/Global/CartanLocalIsometry.lean` ONLY to ADD a constructor/alternative theorem (no changes to existing statements or names; downstream must keep compiling): a mediating theorem deriving the expansion-hypothesis bundle from the interval-scoped facts, i.e. FROM `HasConstantSectionalCurvature3 g 1` (+ the ball/radius side conditions the interval facts carry). STUDY FIRST: (a) the exact expansion-bundle structure in `CartanLocalIsometry.lean`; (b) which interval facts supply each field (radial: ray-law derivative; transverse: `expAt_chart_initialVelocity_hasDerivAt_eq_sin_smul` + `jacobi_position_eq_sin_smul_on_Icc`; cross: Gauss orthogonal law; the endpoint evaluation converts interval facts at `t = ‖v‖`). If the bundle demands MORE than the facts give (e.g. uniformity the interval versions lack), WEAKEN the bundle via a new alternative def + a new local-isometry variant proved from rigid-11's proof pattern — additive only.

Deliverables: the mediating constructor(s) in `CartanLocalIsometry.lean` (additive) + a NEW file `Poincare/Global/CartanIsometryFinal.lean` with: the source expansion for constant-curvature-1 `g`, the target expansion for `roundSphereMetric3` (witness), and 🎯 THE UNCONDITIONAL LOCAL ISOMETRY. Strict-partial: the constructor alone is valuable; ONE isolated statement max. Report `harness/reports/M5-rigid-13_{done|blocked}.md`.

No vacuous wrappers. Verify: `lake build Poincare.Global.CartanLocalIsometry Poincare.Global.CartanIsometryFinal` (BOTH must pass) and report the actual result. Commit your work.
