Read harness/worker_contract.md first and obey it strictly.

# Task M5-glob-4: THE LOCAL ISOMETRY IS PROVEN — feed the continuation

Context: 🎉 `Poincare.RigidityComplete.cartanMap_isLocalIsometry` (`RigidityComplete.lean` — READ the statement): curvature-only; for every closed simply-connected constant-curvature-1 `g`, anchors, alignment: on a punctured shrunk ball, equivalences `A B` with `HasStrictFDerivAt (cartanChartMap …) (cartanChartDifferential L A B)` AND the full pullback equality. THE CONSUMERS (staged LONG ago, awaiting exactly this): (1) `CartanContinuation.lean` (glob-2 — germ determinacy, re-anchored germs, two-step continuation: its staging hypothesis was an `EqOn` differential-action surface — READ its exact staged hypothesis and check whether the strict-derivative + pullback statement discharges or adapts to it); (2) `CartanChain.lean` (glob-3 — subdivision + chain iteration; its blocked report cited the normal-image/germ-source connection + alignment determinacy — the proven theorem supplies the differential data); (3) `CoveringSkeleton.lean` (PROVEN: compact `IsLocalHomeomorph Φ` ⟹ covering ⟹ `M ≃ₜ RoundSphere3` over simply-connected base) — the LOCAL HOMEOMORPHISM property of the Cartan map follows from the strict derivative with EQUIVALENCE differential (the inverse function theorem — the `ExponentialLocalHomeo.lean` pattern: `HasStrictFDerivAt` + `E ≃L` ⟹ `toPartialHomeomorph`). THE TASK: (a) the local-homeo corollary of the proven theorem (the IFT application — `cartanMap` restricted to a small ball is a partial homeomorphism); (b) feed/adapt the continuation staging hypotheses; (c) as far toward the global `Φ` as closes. Strict-partial per stage; ONE isolated statement max. Report `harness/reports/M5-glob-4_{done|blocked}.md`.

Deliverables in a NEW file `Poincare/Global/IsometryConsumers.lean` (do NOT edit existing files, incl. `Poincare.lean`).

No vacuous wrappers. Verify: `lake build Poincare.Global.IsometryConsumers` and report the actual result. Commit your work.
