Read harness/worker_contract.md first and obey it strictly.

# Task M3-predicates-7: the HasFDerivAt layer → finish TraceMetricVariationDerivAt

Read `harness/reports/M3-predicates-6_blocked.md`. On main: the ENTIRE cancellation algebra is proven (`spatialMetricDerivAt_eq_leviCivita`, `spatialMetricDualVectorDerivAt` + `..._inner_apply`); what remains is pure normed-space calculus in the fixed fiber E (TangentSpace = E by rfl):

1. **`hasFDerivAt_metricDualVectorAt`**: `y ↦ metricDualVectorAt g y φ` has derivative `spatialMetricDualVectorDerivAt g x v φ` at x (in v). Route: `metricDualVectorAt_eq_metricRaiseContinuousAt` rewrites it as the continuous raise map = inverse-of-metric-CLM applied to φ. The derivative of an operator inverse: Mathlib `HasFDerivAt.clm_inverse`-family (`ContinuousLinearMap.inverse` differentiability at units — `differentiableAt_inverse`, `fderiv_inverse`; ModelLaplacian's `hasFDerivAt_inverse_raise` did this once — mine it, fibers are E). Prerequisite: `y ↦ (metric CLM at y)` is differentiable — from `contMDiff_inner`/the metric's smoothness (the `mdifferentiableAt_gradient` proof in Global/Laplacian.lean differentiated exactly this object — reuse its lemmas).
2. **Trace product rule**: differentiate `y ↦ Σᵢ h y eᵢ (metricDualVectorAt g y (coord i))` via `HasFDerivAt.mul`/`.comp` + `VariationSpatiallyDifferentiableAt h x` (fixed-vector derivative of h) + step 1. NOTE: h y takes TM-y arguments but everything is E — apply the fixed-vector hypothesis at eᵢ and pair with the varying second slot via bilinearity + `HasFDerivAt.clm_apply` (h y eᵢ is a CLM in the second slot? — check h's actual type `∀ y, TM y → TM y → ℝ` plain-function; if bilinearity-as-CLM is needed, the `VariationSpatiallyDifferentiableAt` class or the raisedEndo packaging from predicates-4's kernel provides the CLM form — use the kernel's endomorphism field, whose derivative is exactly step 1 + the h♭ derivative).
3. **Assemble**: step 2's derivative + the cancellation algebra (`spatialMetricDualVectorDerivAt_inner_apply` + swap machinery) = contracted `covTensor2DerivAt` → **discharge `TraceMetricVariationDerivAt`** (under VariationSpatiallyDifferentiableAt + honest extras). Adapter where consumed; restate `deltaGamma_innerTrace_eq` modulo classes only; notes update.

No sorry/axiom; partials + report = success. `lake build Poincare.Global.ScalarVariation Poincare.Global.ScalarEvolution`, report names.
