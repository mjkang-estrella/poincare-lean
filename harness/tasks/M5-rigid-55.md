Read harness/worker_contract.md first and obey it strictly.

# Task M5-rigid-55: FORENSIC AUDIT — which chart does the sin formula live in?

Context: TWO refutations (`ChristoffelCollapse.lean`, `CollapseOnRay.lean` + reports 53/54): the coordinate harmonic `sin(st)·w` does NOT solve the hosted PL linearized system, at general states NOR at ray states, in the raw stereographic chart. YET `jacobi_position_eq_sin_smul_on_Icc` (`JacobiOscillator.lean`) IS proven. THE AUDIT (before any new proof): (1) READ `JacobiOscillator.lean`'s statement and hypotheses END TO END: in WHICH chart/coordinates does its `J` live (raw extChartAt? the cutoff-one zone chart from `ChartCurvatureBridgeZone/ZoneClose.lean`? an abstract system under hypotheses that encode the zone)? WHICH second-order equation does it assume (`coordinateCovariantJacobiSecond...` — the COVARIANT oscillator: J'' means the covariant second derivative, NOT the coordinate one — then sin·w solves the COVARIANT equation, and the coordinate claim is FALSE precisely as refuted!). (2) READ how `CartanIsometryTheorem.lean` discharged the oscillator hypotheses along the cutoff-one flow — what object satisfied them (the covariant state, presumably). (3) THE LIKELY TRUE ROUTE: the hosted linearized system's solution `Ψ` (which IS the flow derivative) relates to the COVARIANT Jacobi field through the state bridge (`JacobiNormClose.lean`'s `D = K + Γ(z)(V,J)`) — the endpoint VALUE `(Ψ_w T).1` equals the covariant field's position (positions agree; only derivatives differ by Γ) — so `jacobi_position...` (a POSITION statement) may apply to `Ψ.1` DIRECTLY with the right hypothesis feed, WITHOUT any coordinate-harmonic claim. Check whether rigid-48's `hendpoint` + the position formula give the action equation directly, bypassing hΦderHosted/haccCollapse entirely.
Deliver: (a) the audit findings VERBATIM in the report (chart, equation form, hypothesis shapes); (b) if route (3) closes: the action equation via the position formula in a NEW file `Poincare/Global/PositionRoute.lean` + as much of the composition as follows; (c) if not: the ONE true missing statement.

Deliverables: NEW file only (do NOT edit existing files, incl. `Poincare.lean`); if no theorem is provable this round, the file may hold the audit's formalizable corollary or be omitted (report-only). Report `harness/reports/M5-rigid-55_{done|blocked}.md`.

No vacuous wrappers. Verify: `lake build Poincare.Global.PositionRoute` if created (else state so) and report the actual result. Commit your work.
