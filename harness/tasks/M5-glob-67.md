Read harness/worker_contract.md first and obey it strictly.

# Task M5-glob-67: THE SELECTOR ASSEMBLY — the F-transition law, at last

Context: `harness/reports/M5-glob-66_done.md` (READ FIRST). THE PACKAGE EXISTS EVERYWHERE: `exists_rescaled_hosted_thirdVariation_centered_membership_clm_package` (`CenteredMembership.lean` — a direct `TheSelector.HostedCLMPackage` at EVERY hosted endpoint time). THE ASSEMBLY: (1) `TheSelector`'s D-selector now unconditional (the package fed) + `selected_endpoint_gronwall_bound` fires (its endpoint-spec hypotheses from the package's specs); (2) the indexed packages (`q ↦ ζ/Ω/D` with the transferred Grönwall bounds — `TheSelector/TwoConnectors`' selectors + the bound); (3) feed `IndexedSelection.exists_cartanChartMap_christoffelAt_F_transition_law_of_indexed_endpoint_gronwall_packages` (both sides — the sphere by the same generic instantiation) → 🎯 THE F-TRANSITION LAW UNCONDITIONAL (curvature-only, on the punctured shrunk ball). If ONE shape resists, isolate verbatim. Report `harness/reports/M5-glob-67_{done|blocked}.md`.

Deliverables in a NEW file `Poincare/Global/SelectorAssembly.lean` (do NOT edit existing files, incl. `Poincare.lean`).

No vacuous wrappers. Verify: `lake build Poincare.Global.SelectorAssembly` and report the actual result. Commit your work.
