Read harness/worker_contract.md first and obey it strictly.

# Task M5-rigid-42: export the Ψ family — the last construction

Context: `harness/reports/M5-rigid-41_blocked.md` (READ FIRST). PROVEN: the action-equation theorem (`CartanActionEquations.lean` — CLM action from radial action + rescaled harmonic hypothesis), the strict-derivative existence given a family (`CartanIsometryClose.lean`), the pinned scalars, the hosted conversion. THE LAST CONSTRUCTION: export the CONCRETE hosted `Ψ` family — for each `v` in the shrunk ball, the linearized solution family at `v`'s hosted `(u,T)` data with the properties the two consumer theorems need (the linearized-family hypotheses of `exists_shrunk_..._of_linearized_family` and the radial-action/rescaled-harmonic hypotheses of the action theorem). WHERE IT LIVES: `GeodesicFlowDerivative.lean` constructed the linearized solutions at hosted data (its flow-derivative theorem's proof HAS the family — check what is exported vs internal); `GeodesicLinearized.lean` the existence/uniqueness; the radial action is the ray-law time-derivative (`CartanHomogeneity.lean`'s hosted endpoint derivative); the rescaled harmonic property is `jacobi_position_eq_sin_smul_on_Icc`'s hypothesis package (`JacobiOscillator.lean` — its own hypotheses were discharged along the cutoff-one flow in `CartanIsometryTheorem.lean`'s proof — REUSE that discharge). Then: instantiate BOTH consumers, source AND target (`roundSphereMetric3` — same generic route + the witness) → the unconditional `A`/`B` with their action equations → feed `cartanMap_isLocalIsometry_on_punctured_normalBall_of_hosted_scale_endpoint_pairings` (`CartanScaleGeneric.lean`) with the source blocks (pairing facts at `(u,T)`, `CartanIsometryPackage.lean`) → 🎯 THE LOCAL ISOMETRY.

Deliverables in a NEW file `Poincare/Global/LinearizedFamilyExport.lean` (do NOT edit existing files, incl. `Poincare.lean`). Strict-partial per item; ONE isolated statement max. Report `harness/reports/M5-rigid-42_{done|blocked}.md`.

No vacuous wrappers. Verify: `lake build Poincare.Global.LinearizedFamilyExport` and report the actual result. Commit your work.
