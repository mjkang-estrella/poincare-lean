Read harness/worker_contract.md first and obey it strictly.

# Task M5-glob-47: the field producer — sourceD and targetD constructed

Context: `harness/reports/M5-glob-46_blocked.md` (READ FIRST — the VERBATIM `sourceD/targetD` field shapes). PROVEN: the complete consumer (`PackageLands.lean` — fields ⟹ THE F-TRANSITION LAW); every ingredient: the residual theorem (`SecondFlowDerivative.lean`) with ALL its hypotheses exportable (`SecondDischarge` ODE facts, `SecondVariation` PL solutions, `AugmentedPackage` remainders, `AugmentedC1/Dependence` Lipschitz). THE PRODUCER: (1) DEFINE the fields — `sourceD q :=` the second-variation endpoint at `q`'s hosted datum (the PL solution selection — the `LinearizedFamilyExport/PLPackages` choice pattern at the augmented system); (2) their `HasFDerivAt` facts — FEED the residual theorem at each ball point (the hypotheses instantiated: the ODE facts at `q`, the remainders on the tube, the Lipschitz) ⟹ `HasFDerivAt (flow) (sourceD q) q`-shaped — the field IS a derivative field; (3) continuity of `q ↦ sourceD q` — from the dependence estimates (the second-variation solutions depend Grönwall-continuously on the base — `AugmentedDependence`'s pattern applied to the second-variation system) OR check whether `ExpChartC2`'s package needs only the `HasFDerivAt` facts + the C¹-of-field via `contDiffAt_succ_iff_hasFDerivAt` (which needs the field `ContDiffAt 1` — one more level: if genuinely needed, the THIRD variation... ⚠️ CHECK THE EXACT DEMAND FIRST — `contDiffAt_succ_iff_hasFDerivAt` needs the field only `ContDiffAt (n-1) = ContDiffAt 1` for C², i.e. the field differentiable — which is the residual theorem at the SECOND-variation system... if a third level threatens, ISOLATE and report — do not silently assume). (4) the sphere side identically. FEED → 🎯 THE F-TRANSITION LAW. Strict-partial; ONE isolated statement max. Report `harness/reports/M5-glob-47_{done|blocked}.md`.

Deliverables in a NEW file `Poincare/Global/FieldProducer.lean` (do NOT edit existing files, incl. `Poincare.lean`).

No vacuous wrappers. Verify: `lake build Poincare.Global.FieldProducer` and report the actual result. Commit your work.
